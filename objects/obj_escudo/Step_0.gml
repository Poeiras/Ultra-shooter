/// @description posição do escudo

x = obj_player.x;
y = obj_player.y;

// transparência do escudo
if (image_alpha = 0)
	{
		image_alpha += 0.1;
	}

if (image_alpha = 1)
	{
		image_alpha += 0;
	}

if (image_index = 0)
	{
		image_alpha -= 0.1;
	}