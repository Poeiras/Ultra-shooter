/// @description config da tremedeira

// Variáveis locais
var camx = random_range(-forca,forca);
var camy = random_range(-forca,forca);

// Configuração da camera 

camera_set_view_pos(view_camera[0],camx,camy);